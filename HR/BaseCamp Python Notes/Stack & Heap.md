## Stack
- The stack is a region of memory that stores **temporary variables** created by each function.
- It operates on a **Last-In First-Out** principle. Items that are added most recently, are removed first.
## Heap
- The heap is a larger capacity.
- It is also a bit slower.