> [!info] Reference
> [[Introducing Python - Book]] - Chapter 8.
# Sets
- A collection of **unique** items.
## Create with `set()`
- Converting a [[Lists|list]] or [[Tuples|tuple]] to a set with `set()`, removes any duplicate values.
## Add an item with `add()`
## Delete an item by value with `remove()`
## Set methods
- `union()`
- `difference()`'
- `intersection()`
- `symmetric_difference()`