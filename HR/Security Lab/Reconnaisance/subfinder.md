Voorbeeld van passive [[Reconnaisance|reconnaisance]].
```bash
subfinder -d <domain> -all
```
```bash
subfinder -d <domain> -silent -o subs.txt
```
- `-d` - Domein om op te zoeken.
- `-silent` - Niet extra logs printen, alleen subdomains.
- `-o` - Output resultaten naar een file.