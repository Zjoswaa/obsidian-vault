[CrackStation](https://crackstation.net/) is een website waar ~1.5 miljard bekende en gebruikte hashes kunnen worden opgezocht.

`hashid` en `hash-identifier` zijn tools op Kali Linux die proberen het algoritme  te achterhalen dat is gebruikt om de hash te maken.